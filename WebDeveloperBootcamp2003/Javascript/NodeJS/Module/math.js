const add = (x, y) => x + y;

const PI = 3.14159

const square = x => x * x;

class test {
    constructor() {
        //test
    }
}

const math = {
    add: add,
    PI: PI,
    square: square,
    test: test
};
module.exports = math;

